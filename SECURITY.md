# 安全策略 (Security)

「记忆黑洞」以**数据绝对安全、纯本地、隐私优先**为核心。以下是我们如何保护你的数据，以及如何向我们报告安全问题。

## 数据与隐私承诺

| 承诺 | 说明 |
|---|---|
| 纯本地存储 | 所有记忆数据保存在 EXE 同目录的 `.memoryblackhole` 文件夹，**不上云、不联网上传**。 |
| 媒体原样存储 | 图片/音频/视频保持原格式；≤500MB 复制到本地 media 目录，超限仅存原始路径。 |
| 密码保护 | 密码只保存 PBKDF2 哈希 + 随机盐，**绝不存储明文**；退出记忆空间后再次进入须重新验证。 |
| 无追踪 | 不收集使用统计、不上报日志、无远程遥测。 |

## 支持的版本

安全修复只覆盖最近发布的版本。建议始终保持最新版本。

## 报告安全漏洞

本项目当前为私有仓库 / 个人工具。若发现安全问题，**请勿公开发布**（不要在 Issue 中提交敏感漏洞细节）。

请将漏洞私密提交给维护者（作者 vicz / GitHub: aicbbuu）。报告时尽量包含：

- 漏洞类型与影响；
- 复现步骤；
- 会否泄露用户记忆数据；
- 建议修复方式。