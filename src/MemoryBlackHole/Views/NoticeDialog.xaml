<Window x:Class="MemoryBlackHole.Views.NoticeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        MinWidth="320" MinHeight="170" MaxWidth="500" MaxHeight="300"
        SizeToContent="WidthAndHeight" WindowStartupLocation="CenterOwner"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        AllowsTransparency="True" Background="Transparent">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border Background="#11182A" BorderBrush="#394766" BorderThickness="1" CornerRadius="16" Padding="24">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <TextBlock x:Name="TitleText" Text="记忆黑洞" FontSize="19" Foreground="White" FontWeight="SemiBold"/>
            <TextBlock x:Name="MessageText" Grid.Row="1" TextWrapping="Wrap" Foreground="#B8C1D8" FontSize="13" Margin="0,12,0,12"/>
            <Button Grid.Row="2" Content="知道了" Width="82" HorizontalAlignment="Right" Click="Ok_Click"
                    Background="#6D5DF7" Foreground="White" BorderThickness="0" Padding="10,7"/>
        </Grid>
    </Border>
</Window>
