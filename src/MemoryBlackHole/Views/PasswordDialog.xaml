<Window x:Class="MemoryBlackHole.Views.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="验证记忆空间" Width="320" Height="220" WindowStartupLocation="CenterOwner"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        AllowsTransparency="True" Background="Transparent" Foreground="#E9ECF5">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border BorderBrush="#303B59" BorderThickness="1" CornerRadius="16" Background="#0F1424" Padding="22">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/><RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="HeaderText" Text="进入记忆空间" FontSize="20" FontWeight="Light" Foreground="White"/>
            <TextBlock x:Name="DescriptionText" Grid.Row="1" Text="请验证密码后继续" FontSize="12" Foreground="#8991A8" Margin="0,6,0,14"/>
            <PasswordBox x:Name="PasswordBox" Grid.Row="2" Height="38" Background="#171D30" BorderBrush="#303B59"
                         Foreground="White" Padding="11,7" FontSize="14" VerticalContentAlignment="Center"/>
            <StackPanel x:Name="ConfirmPanel" Grid.Row="3" Margin="0,10,0,0">
                <TextBlock x:Name="ConfirmLabel" Text="再次输入密码" Foreground="#8991A8" FontSize="11" Margin="0,0,0,4"/>
                <PasswordBox x:Name="ConfirmBox" Height="38" Background="#171D30" BorderBrush="#303B59"
                             Foreground="White" Padding="11,7" FontSize="14" VerticalContentAlignment="Center"/>
            </StackPanel>
            <TextBlock x:Name="ErrorText" Grid.Row="4" Foreground="#FF7E9E" FontSize="11" Margin="0,8,0,0"/>
            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="取消" Width="76" Margin="0,0,9,0" Click="Cancel_Click"
                        Background="Transparent" Foreground="#C5CBE0" BorderBrush="#34405D" BorderThickness="1" Padding="9,7"/>
                <Button x:Name="OkButton" Content="确认" Width="92" Click="Ok_Click"
                        Background="#6D5DF7" Foreground="White" BorderThickness="0" Padding="9,7"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
